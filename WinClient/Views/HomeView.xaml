<UserControl x:Class="WinClient.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinClient.Views"
             mc:Ignorable="d" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid>
        <Grid.ColumnDefinitions></Grid.ColumnDefinitions>
        <Grid.RowDefinitions></Grid.RowDefinitions>
        <md:Card Width="500" Height="300" BorderThickness="2">
            <StackPanel>
                <TextBlock Text="Email" Width="150" Style="{StaticResource MaterialDesignBody1TextBlock}"></TextBlock>
                <TextBox Text="{Binding Email}" Width="150" Height="30" Style="{StaticResource MaterialDesignFilledTextBox}"></TextBox>
                <TextBlock Text="Password" Width="150" Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                <TextBox Text="{Binding Password}" Width="150" Height="30" Style="{StaticResource MaterialDesignFilledTextBox}"></TextBox>
                <Button Margin="0 15 0 0" Width="150">Login</Button>
            </StackPanel>
        </md:Card>
    </Grid>
</UserControl>
